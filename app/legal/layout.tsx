import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "規約・ポリシー・特定商取引法に基づく表記",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
